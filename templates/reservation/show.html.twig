{% extends 'baseAdmin.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <div class="content-header sty-one">
      <h1>Reservations</h1>
    </div>
      <div class="content">
         <div class="info-box">
         <div class="table-responsive">
    

    <table class="table">
        <tbody>
            <tr>
                <th>Titulaire</th>
                <td>{{ reservation.user.nom }} / {{ reservation.user.prenom }} </td>
            </tr>
            <tr>
                <th>Film</th>
                <td>{{ reservation.projection.video.titre }}</td>
            </tr>
             <tr>
                <th>Le cinema</th>
                <td>{{ reservation.projection.cinema.nom }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reservation.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_index') }}" class="btn btn-warning">Retour à la liste</a>

    {{ include('reservation/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
