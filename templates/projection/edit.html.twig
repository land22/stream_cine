{% extends 'baseAdmin.html.twig' %}

{% block title %}Modifier la projection{% endblock %}

{% block body %}
    <div class="content-header sty-one">
      <h1>Projections</h1>
      <ol class="breadcrumb">
        <li><a href="{{ path('app_projection_index') }}" class="btn btn-warning">Retour à la liste</a></li>
      </ol>
    </div>
      <div class="content">
         <div class="info-box">
         <div class="table-responsive">

    {{ include('projection/_form.html.twig', {'button_label': 'Modifier'}) }}


    {{ include('projection/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
