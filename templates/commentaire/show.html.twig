{% extends 'baseAdmin.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <div class="content-header sty-one">
      <h1>Commentaire </h1>
      <ol class="breadcrumb">
        <li><a href="{{ path('app_commentaire_index') }}" class="btn btn-warning">Retour à la liste</a></li>
      </ol>
    </div>
      <div class="content">
         <div class="info-box">
         <div class="table-responsive">

    <table class="table">
        <tbody>
            <tr>
                <th>Utilisateur</th>
                <td>{{ commentaire.user.nom }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('commentaire/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
