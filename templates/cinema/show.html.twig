{% extends 'baseAdmin.html.twig' %}

{% block title %}Cinema{% endblock %}

{% block body %}
    <div class="content-header sty-one">
      <h1>Liste Cinema </h1>
      <ol class="breadcrumb">
        <li><a href="{{ path('app_cinema_index') }}" class="btn btn-warning">Retrour à la liste</a></li>
      </ol>
    </div>
      <div class="content">
         <div class="info-box">
         <div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ cinema.nom }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ cinema.localisation }}</td>
            </tr>
        </tbody>
    </table>
   <p>
    <a href="{{ path('app_cinema_edit', {'id': cinema.id}) }}" class="btn btn-warning">Modifier</a>
    </p>
    {{ include('cinema/_delete_form.html.twig') }}
     </div>
    </div>
    </div>
{% endblock %}
