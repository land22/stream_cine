{% extends 'baseAdmin.html.twig' %}

{% block title %}Cinema{% endblock %}

{% block body %}
    <h1>Liste des Cinemas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ cinema.nom }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ cinema.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cinema_index') }}">Retrour à la liste</a>

    <a href="{{ path('app_cinema_edit', {'id': cinema.id}) }}">Modifier</a>

    {{ include('cinema/_delete_form.html.twig') }}
{% endblock %}
